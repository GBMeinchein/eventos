<%= form_for(@confirmado) do |f| %>
  <% if @confirmado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@confirmado.errors.count, "error") %> prohibited this confirmado from being saved:</h2>

      <ul>
      <% @confirmado.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :evento_id %><br>
    <%= f.number_field :evento_id %>
  </div>
  <div class="field">
    <%= f.label :login_id %><br>
    <%= f.number_field :login_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
